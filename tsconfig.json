{
	"extends": "@zayne-labs/tsconfig/bundler/next",
	"compilerOptions": {
		"baseUrl": ".",
		"paths": {
			"@/*": ["./*"]
		}
	},

	"include": ["**/*.ts", "**/*.tsx", "public/**/*.ts", ".next/types/**/*.ts"],
	"exclude": ["node_modules", ".next"]
}
