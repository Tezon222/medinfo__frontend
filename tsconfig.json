{
	"extends": "@zayne-labs/tsconfig/bundler/next",
	"compilerOptions": {
		"baseUrl": ".",
		"paths": {
			"@/*": ["./*"]
		}
	},

	"include": ["**/*.ts", "**/*.tsx", "public/**/*.tsx", ".next/types/**/*.ts"],
	"exclude": ["node_modules", ".next"]
}
